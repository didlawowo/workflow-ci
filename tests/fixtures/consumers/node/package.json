{
  "name": "workflow-ci-node-consumer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "node --test --test-reporter=junit --test-reporter-destination=coverage-reports/junit.xml",
    "lint": "node --check test/add.test.js"
  }
}
